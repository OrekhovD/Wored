# Проверенные исходные сведения и пределы проверки

Дата подготовки: 14.09.2026, Asia/Bangkok. Полный Git snapshot и время генерации — BASELINE.json. Источник: рабочее дерево `D:\WORED`, не чистый checkout и не экспорт контейнера.

## Проверено при подготовке этого пакета

- Прочитаны текущие `docs/HERMES-EXECUTION-REVIEW-20260914.md`, `docs/TRADING-SIMULATOR-AGENT-CONTRACT.md`, прежний пакет `TASOCHKI/HERMES-WORED-TRADING-DAY`, функции paper API/store/market и регистрация задач collector.
- HEAD `4a438be`; существуют изменённые и untracked runtime-файлы. Поэтому одного commit недостаточно для воспроизводимости.
- В просмотренной цепочке Daily Session используется отдельный session engine; WebUI paper API/store — другой контур. Наличие advisory-модулей `paper_agents.py` не подтверждает исполнение ими торговых команд. Полный поиск фактических текущих регистраций входит в discovery Hermes.
- Существующий `docker-compose.qa.yml` использует `wored_qa`, tmpfs PostgreSQL и отдельный QA project; это основа изолированных DB-проверок.
- В этой задаче создаётся только пакет ТЗ. Docker не перезапускался, миграции не применялись, активная сессия и её актуальная причина нуля позиций не проверялись.

## Ранее полученные результаты: не текущий runtime

- В предыдущем проходе 14.09 примерно в 18:30 Bangkok шесть контейнеров были running; три health endpoint вернули 200. Это историческое свидетельство инфраструктуры, не исполнения стратегии.
- Позднейшие read-only Docker/SQL проверки были отклонены автоматическим контролем разрешений из-за лимита самого сервиса проверки. Обход не выполнялся. Причина отказа не доказывает неисправность Docker и может уже не действовать.
- Локально добавлены execution heartbeat и причины ожидания, убрана обрезка статуса, исправлен выход из cooldown и разделены цена сигнальной свечи и текущая цена исполнения. Применение этого последнего диагностического патча работающими процессами не подтверждено.
- Для того патча ранее выполнено: `python -m pytest tests/test_execution_status.py tests/test_session_plan_contract.py tests/stabilization/test_execution_contracts.py tests/stabilization/test_snapshot_consumers.py -q -p no:cacheprovider` → 42 passed, 15 skipped (DB), одно pytest_asyncio/Python 3.14 предупреждение. Ruff/mypy нового модуля и компиляция затронутых модулей прошли. Эти проверки не повторялись при упаковке ТЗ и не являются приёмкой новой архитектуры.
- Hermes ранее менял default analyst на `gpt-oss:120b`, output limit 4000 и timeout 90 s. Env может переопределять default. Фактическая модель подтверждается execution audit, не commit message. Заявление о генерации за 11 секунд независимо не подтверждено.

## Что нельзя выводить из этих сведений

Нельзя утверждать, что текущая сессия получила no_trade, что AI исчерпал квоту, что все новые файлы загружены, что auto runner уже работает или что новая стратегия прибыльна. Нельзя приравнивать прежние manual browser checks к принятому автоматическому циклу. Для каждого такого вывода требуется свежая проверка в соответствующей среде.
