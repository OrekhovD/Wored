# WORED mypy configuration.
# Per-module allowances only: silent "import-untyped" for packages that are
# installed but ship no type stubs / py.typed marker. No strictness changes.
[mypy]

[mypy-asyncpg.*]
ignore_missing_imports = True

[mypy-storage.*]
ignore_missing_imports = True
